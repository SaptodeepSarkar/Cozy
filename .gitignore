# Python
__pycache__/
*.py[cod]
*.egg-info/

# Virtual environments (all three venvs)
.venv/
assistant/.venv/
stt-finetune/.venv/
wakeword/.venv/

# Heavyweight working data (downloads, synthetic bulk audio, caches)
wakeword/work/
wakeword/data/   # downloaded by livekit-wakeword setup (~5 GB)
wakeword/.venv/
# Whisper LoRA training output (regenerable via stt-finetune/scripts/train_lora.py)
checkpoints/

# OS noise
.DS_Store
Thumbs.db

# Model weights (kept out of git, regenerated/fetched separately)
assistant/model/**/*.safetensors
assistant/model/**/*.bin
assistant/model/**/consolidated.safetensors
assistant/sft_log.txt
# Merged model output (kept out, regenerated by sft_qwen.py)
assistant/model/cozy-llm-v1/model.safetensors
# Adapter checkpoints
adapter_model.safetensors
# DPO adapter
cozy-llm-v1-dpo/adapter_model.safetensors

# RLM harness state (generated at runtime, regenerable)
assistant/data/harness/
assistant/data/rlm_children/
artifacts/training_runs/
# Glaive dataset (downloaded by data prep scripts)
assistant/data/hf_glaive/

# Local override with real API keys
.mcp.json.local

# cozy-vision deleted in v1.52 (whole folder removed).
# Versióned model snapshots (regenerable via setup.sh + training scripts).
models/**/model.safetensors
models/**/model.bin
models/**/pytorch_model.bin
models/**/cozy_stt-v1.1/ct2/
# Wakeword training workspace (regenerable via livekit-wakeword run)
wakeword/output_v1.1/
# STT training workspace (regenerable via stt-finetune scripts)
stt-finetune/output/cozy_stt_v1.1/
stt-finetune/output/hf_finetuned_v1.1/
stt-finetune/output/whisper_small_base_ct2/
stt-finetune/checkpoints/
# Backing symlinks (regenerated on first launch if missing)
stt-finetune/output/cozy_stt_v1_ct2_int8
stt-finetune/output/hf_finetuned
