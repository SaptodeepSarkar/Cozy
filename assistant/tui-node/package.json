{
  "name": "cozy-tui",
  "version": "2.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "node --experimental-ffi src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-ffi --experimental-strip-types --test src/*.test.ts",
    "check": "npm run typecheck && npm test"
  },
  "dependencies": {
    "@opentui/core": "0.5.11"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
