# Wake-word pipeline dependencies (PyTorch is installed separately by setup.sh)
piper-sample-generator
numpy>=1.24,<2.0
scipy>=1.11
soundfile>=0.12
audiomentations>=0.34
onnxruntime>=1.17
scikit-learn>=1.3
tqdm>=4.66
requests>=2.31
PyYAML>=6.0
sounddevice>=0.4.6

# NOTE: openwakeword is installed by setup.sh with --no-deps because its
# tflite-runtime dependency ships no wheels for Python 3.12. Cozy only uses
# openWakeWord's ONNX runtime paths, so tflite-runtime is not needed.
